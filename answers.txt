What is a gitignore file and what should it include for a Java project?
  A: gitignore file contains ignore rules.
What does fast-forward in Git mean?
  A: A fast-forward is what Git does when you merge or rebase against a branch that is simply ahead the one you have checked-out.
Why should you have different branches?
  A: It can work on multiple aspects of your project at the same time
In a project how many remote Git repositories do you usually have?
  A: As many as you want but only one remote named "origin".
